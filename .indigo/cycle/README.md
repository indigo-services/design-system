# Indigo Asset Cycle

This cycle is the unfinished asset-replacement segment for the Indigo fork.

Cycle objective:
- Prepare every first-pass Indigo brand asset swap as a single replaceable tranche.
- Keep the current Storybook color tokens unchanged.
- Separate planning/staging from actual asset generation and asset integration.

Cycle outputs already accomplished here:
- Replaceable asset manifest.
- Prompt/spec workspace under `.indigo/brand-assets/`.
- Mirrored asset targets under `.indigo/import-targets/`.
- Current source snapshots under `.indigo/source-assets/`.

Remaining cycle work:
- Generate approved Indigo assets.
- Drop them into `.indigo/import-targets/`.
- Apply the replacements to repo paths.
- Verify Storybook shell and icon stories.

